\setcounter{section}{-1}

\usepackage{tikz}
\usetikzlibrary{shapes,automata,arrows}

\usepackage{booktabs}
\usepackage[nameinlink]{cleveref} 

\usepackage{pgf}
\usepackage{pgfplots}
\pgfplotsset{
  compat        = newest,
  table/col sep = semicolon
}
\usepackage{enumitem}
\setlist{nolistsep} % or \setlist{noitemsep} to leave space around whole list

\usepackage{subcaption}
\usepackage{url}
\usepackage{dblfloatfix} % figure* and table* can now be placed at the bottom of a page
\usepackage{fixltx2e} % prevent the figures from being placed out-of-order with respect to their "non-starred" counterparts
\usepackage{float}
